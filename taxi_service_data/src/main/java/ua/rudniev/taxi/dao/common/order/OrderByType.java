package ua.rudniev.taxi.dao.common.order;

/**
 * Enum class that has fields for simplification of work with order by types when building sql queries
 */
public enum OrderByType {
    ASC,
    DESC
}
