package ua.rudniev.taxi.dao.common.field;

/**
 * Enum class that has fields for simplification of work with field types from repository
 */
public enum FieldType {
    INTEGER,
    STRING,
    BIG_DECIMAL,
    INSTANT,
    DOUBLE
}
